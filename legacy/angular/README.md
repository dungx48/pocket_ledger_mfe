# Legacy Angular Scaffold

This folder contains the previous Angular scaffold that used to live at the frontend root.

The active Pocket Ledger frontend is the Next.js app in:

- `app/`
- `components/`
- `lib/`
- `hooks/`

Keep this folder only for reference while migrating or deleting old Angular code. It is excluded from the active Next.js TypeScript project.
